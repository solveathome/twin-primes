# import-kw-zonegap — the graded Kourbatov–Wolf row for the Z₂ programme, executed under §0a's rule

<!-- ledger
id: Q-import-kw-zonegap
status: ANSWERED
todo: none
question: What does the graded Kourbatov-Wolf row buy the Z2 programme, and does the flagged 0.7574 against 0.7504 agreement corroborate anything?
verdict: A split-cell circularity verdict: the trend formula's raw performance is real, G/Tbar inside [0.78, 1.05] over thirteen decades with zero fitted parameters, but the flagged agreement is adjudicated a range accident, and since Theorem occurs zero times in the source the row can only be priced PUBLISHED-ANCHOR plus WALL-ADDRESS, whose wall is a quantifier: an almost-all over a log-sparse record sequence cannot deliver an every-zone statement.
-->

*(Staging note, 2026-08-21. The row below is PROPOSED for `research/IMPORT-MAP.md`
§2 — the orchestrator applies it; nothing in the live layer is edited by this
pass. Producer, formally embedded: `research/import-kw-01-calibrate.js` (0.2 s;
code-sha256 4e31f30b…, out-sha256 eba668de…). Source re-fetched this pass:
arXiv:1901.03785v4 = Mathematics 7 (2019) 400, sha256
`7d69b3564c38a0d0525eeb6a2184bd0375682d2cc61d619b6c1d9398e8767509` — identical
to the recon's hash (`zonegap-prior-art.md` §5), so both passes read the same
artifact. Load-bearing statements verified AT PAGE IMAGE (pp. 1, 7, 8, 24, 27
rendered and read as images, not extracted text; the extraction demonstrably
garbles — p. 27's `15⁴/2¹¹` comes out "154/2^11"). Calibration marked per
claim: VERIFIED, MEASURED, ADJUDICATED.)*

## 0. The answer

**The row lands as a split-cell circularity verdict and a calibration that
banked two things: the trend formula's raw performance is real (G/T̄ inside
[0.78, 1.05] over thirteen decades, zero fitted parameters), and the
recon-flagged "0.7574 vs 0.7504 agreement" is ADJUDICATED a range accident —
a ceiling coefficient and a dimensionless load passing each other, not a
corroboration.** Nothing in the source is a theorem ("Theorem" occurs zero
times, re-verified at §4), so the import can never be priced as THEOREM or
DERIVED-CONSTANT; it is a PUBLISHED-ANCHOR plus a WALL-ADDRESS, and the wall
it addresses is a quantifier: their strongest conjecture is an "almost all"
over a log-sparse record sequence, which cannot deliver an every-zone
statement even if granted in full.

## 1. THE PROPOSED ROW (for IMPORT-MAP.md §2, as row 14)

> | 14 | maximal-gap heuristics for prime k-tuples | **NO THEOREM EXISTS TO
> IMPORT — the framework is wholly conjectural.** Statements at page image:
> trend `G_c(x) ∼ (x/π_c(x))·(log π_c(x) + O_k(1))` (abstract, p. 1); upper
> trend `T̄_c(x) = ā_c(x)·log(x/ā_c(x))`, `ā_c(x) = (ϕ_{k,H}(q)/C_{k,H})·log^k x`
> (Defs 4–5, eqs (12), (14), pp. 7–8); **Generalized Cramér: "Almost all
> maximal gaps `G_c(p)` satisfy `G_c(p) < C_{k,H}^{−1} ϕ_{k,H}(q) log^{k+1} p`"**
> (eq (20), p. 8); **Generalized Shanks: the same right side as the
> asymptote** (eq (21), p. 8); trend conjecture, positive proportion in
> `[T_c, T̄_c]` and `G_c − T̄_c` changes sign infinitely often (eq (19), p. 8);
> Gumbel rescaling `h = (G_c − T_c)/a_c` "a possible limit law … an open
> question" (eq (48), p. 16; p. 24); `N_c(x) < C log x`, `C > k+1` (p. 24);
> `C_{2,H} = 2∏p(p−2)/(p−1)² ≈ 1.32032363` (p. 27, = 2C₂, ceiling coefficient
> `1/C_{2,H} = 0.7574` at `q = 2, ϕ = 1`). Kourbatov–Wolf, *Mathematics* 7
> (2019) 400 = arXiv:1901.03785 **[SOURCED, verbatim, at page image;
> CONJECTURE-ONLY — the paper contains zero theorems]** | `Z₂(p)`'s envelope
> process — CUSTODY 3's identity `env(p)` = largest record wholly below
> `p′²`, EXACT at all 27,292 zones (`zonegap-01.js`), makes the OBJECT
> identification exact; the zone indexing is OURS (§2 below) | the
> staircase-correction model `zonegap-01.md` §7 names as not-reached;
> prereg band-setting for the next sweep; guard duty on measurements | fit:
> **EXACT-IDENTITY at the object, CONJECTURE-ONLY at every statement** |
> circularity: **SPLIT — CIRCULAR in any proof chain, CLEAN as instrument**
> (the binding cell, §3 below) | payoff: PUBLISHED-ANCHOR + WALL-ADDRESS
> (the almost-all quantifier wall) | 4 h | **LANDED 2026-08-21** —
> parameter-free calibration ran (`research/import-kw-01-calibrate.js`):
> `G/T̄ ∈ [0.78, 1.05]` from record 10 to record 82 (p ~ 6·10³ to 7·10¹⁶),
> records split 33/37/12 below-`T_c`/inside/above-`T̄`, 15 sign changes of
> `G − T̄`; 0 of 82 records breach eq (20), ceiling load creeps
> 0.544 → 0.685 → 0.759 by ladder thirds (the Shanks shape, → 1); **the
> recon's "their 0.7574 vs our 0.7504" is a RANGE ACCIDENT** (coefficient
> vs load; the same load statistic reads 0.8463 at record 75 and tends to 1
> under their own eq (21)); band check `c3` vs `T̄(p²)/ln³p`: measured/trend
> 0.943, 0.904, 0.921, 0.867 (over-read 6–13%, `T_c` bracketing below);
> Gumbel sign-level consistent only (skew +0.588 on a correlated
> running-max path, not their ensemble); `history/staging/import-kw-zonegap.md` |

**Grading note for the map's legend (proposed, one sentence).** The map's
payoff vocabulary has no grade for a conditional-heuristic framework, and
this row is the first wholly-conjectural source to enter: the convention
proposed is that such an import is priceable ONLY as PUBLISHED-ANCHOR and/or
WALL-ADDRESS, its theorem column carries an explicit **CONJECTURE-ONLY**
marker beside the [SOURCED] grade, and THEOREM / DERIVED-CONSTANT are
structurally unavailable (every constant in the source is
conjecture-conditional, including the 0.7574).

## 2. The hypothesis map — ALL the hypotheses, and where the translation leaks

The corpus lesson (campaign rules, 2026-08-19): read the unquoted hypotheses.
KW's conjectures rest on THREE, two of them explicit and one buried in the
derivation, plus a definitional clause that matters to us:

1. **The k-tuple infinitude conjecture** (§2.1.2, p. 6): every H-allowed
   residue class contains infinitely many k-tuple starters. At `H = (0,2)`,
   `q = 2` this IS the Twin Prime Conjecture. Without it `G_c(p)` is
   eventually undefined. Remark 2(ii): follows from Bateman–Horn.
2. **The k-tuple equidistribution conjecture** (eq (6), p. 6):
   `π_c(x) = (C_{k,H}/ϕ_{k,H}(q))·Li_k(x) + O_{η,H}(x^η)`, `η < 1` — a
   POWER-SAVING error term, the GRH-analogue. **Remark 3(ii), read at page:
   "compatible with the Bateman–Horn and Hardy–Littlewood k-tuple
   conjectures but does not follow from them."** This corrects the recon's
   §1 line "which they derive from Bateman–Horn": the infinitude follows
   from BH; the equidistribution is an extra assumption STRONGER than both
   classical conjectures. Every trend formula quietly consumes it via
   `x/π_c(x) ≈ a_c(x)` (p. 7).
3. **The exponential-spacing (Poisson-model) hypothesis**, unquoted in the
   conjecture statements: the EVT derivation of the trend (eq (11), p. 7)
   models twin gaps as iid exponential intervals. The whole shape
   `ā·log(x/ā)` is this hypothesis wearing extreme value theory; KW never
   restate it as a condition on eqs (19)–(21).
4. **The indexing clause** (p. 8, last line of §2.3.1): "Here `G_c(p)`
   denotes the maximal gap that ends at the prime p" — the ceiling reads at
   the END prime. Our sweep's convention `s_end = gapStart + Z2` matches
   (`zonegap-01.js` header); no leak here, recorded because a ±convention
   slip at this cell would silently move every load figure.

**The dictionary, zone coordinates (the translation is OURS — the recon's §3
verified nobody indexes these gaps by a window, so no cell below has a
published counterpart):**

| theirs | ours | status |
|---|---|---|
| `x` (global cutoff) | `s = p′²` (the zone's top; the binding gap lives at `u → 1`) | ours; measured support: last 15 envelope steps at `u = 0.9992..1.0000` |
| `G_c(x)`, records below `x` | `Z₂(p) = env(p)`, largest record WHOLLY inside `(p, p′²)` | EXACT at all 27,292 zones (CUSTODY 3) |
| `π_c(x)` | in-zone pair count (sweep: 224,376,048 pairs total) | ours |
| `q, r, ϕ_{k,H}(q)` (residue classes) | no analogue — the zone truncation replaces the residue-class localisation; `q = 2, ϕ = 1` throughout | structural mismatch, stated |
| maximal-gap sequence, one per record | one number per zone, thousands of zones per record | the correlation zonegap-01 §3 measured; never fit as independent points |

**The leakiest translation point, named.** Their "almost all" is quantified
on p. 24: exceptions are "a zero proportion of maximal gaps". The maximal-gap
sequence has conjecturally `< C log x` members below `x` (same page) — a
LOG-SPARSE sequence, so "zero proportion" tolerates infinitely many
ceiling-breaking records. One breaching record wholly inside one zone breaks
that zone. So even granting their entire framework, eq (20) cannot deliver
"every zone holds" — the quantifier gap between *density-1 over a log-sparse
record sequence* and *every p* is unbridgeable by any finite strengthening
they offer, and their 2013 §6 (Granville's `2e^{−γ}` correction, recon §1)
already records the analogous k = 1 ceiling as conjecturally VIOLATED on an
infinite subsequence. The zone lower end `p` leaks too (a record straddling
`p` serves the zone but not `Z₂`), but CUSTODY 3 measured that leak at zero
over the sweep; the quantifier leak is the one that cannot be measured away.

## 3. The circularity pre-check — the row's binding cell

**CIRCULAR in any proof chain.** Hypothesis 1 at `H = (0,2)` IS twin
infinitude; hypothesis 2 is strictly stronger than Hardy–Littlewood by the
authors' own Remark 3(ii). Importing any of eqs (19)–(21), (6), or the
infinitude conjecture as a HYPOTHESIS in an argument toward the Zone
Postulate, TPC, or any `Z₂`/`G₂`/`L` bound is the postulate assuming itself —
the map's definition of CIRCULAR, and worse than the four unmaskings of
2026-08-19 because here the hypothesis is not disguised at all. FORBIDDEN
specifically: (i) any appearance in a proof chain; (ii) citing the trend's
empirical fit as *evidence toward* the postulate (the fit corroborates the
HL/Poisson model, whose twin-relevant content assumes what we would be
proving); (iii) citing the 0.7574/0.7504 proximity for anything (§5, range
accident); (iv) "conditional on Generalized Cramér, the zone is safe" — this
fails TWICE, circularity aside: eq (20)'s quantifier tolerates infinitely
many exceptions (§2), so the conditional statement is not even the postulate.

**CLEAN as instrument, four legal uses:** (a) MODELING/PREDICTION on finite
data — trend curves as reference shapes for the measured envelope, exactly
as `zonegap-01.js` already used the E-form as an estimator control; (b)
PRIOR-SETTING — sealed prereg bands for the next sweep's staircase-correction
model may be centred on KW forms (a prereg band is a bet, not a premise);
(c) GUARD DUTY — the ceiling as a falsifiable published guard on our
measurements (a breach would be publishable AGAINST them; non-breach claims
nothing); (d) PRIOR-ART/ANCHORING — the owning convention for the envelope's
record skeleton, per the recon. The test that separates legal from illegal:
**a KW statement may consume our data; it may never feed a derivation.**

## 4. Spot-verification of the recon (task d)

- **"The string 'Theorem' does not occur" — VERIFIED, sharpened.** Over the
  full extraction of the same-sha artifact: capital "Theorem" 0 occurrences;
  lowercase "theorem" 4, all citations of classical results (PNT p. 1; PNT
  for APs p. 5; Dirichlet p. 5; PNT-as-convention p. 27). Cross-checked
  against page images at pp. 5–8 and 24: every growth statement sits under
  §2 "Heuristics and Conjectures" or the p. 24 Summary's "conjecture"
  bullets. The recon's claim stands with the denominator now stated.
- **The almost-all wording — VERIFIED AT PAGE IMAGE, and the recon
  under-quoted it.** Eqs (20)/(21) read "Almost all maximal gaps `G_c(p)`
  satisfy …" (p. 8, image). What the recon did not carry: p. 24 (image)
  quantifies it — "Exceptionally large gaps … are extremely rare … We
  conjecture that only a zero proportion of maximal gaps are such
  exceptions. A similar observation holds for `G_c(p)` violating (20)." —
  which is what makes the quantifier wall of §2 exact rather than read-in.
- **One recon line corrected:** "their k-tuple conjectures, which they
  derive from Bateman–Horn" (recon §1) — the infinitude does follow from BH
  (Remark 2(ii)); the equidistribution conjecture (6) explicitly does NOT
  (Remark 3(ii)). The row's hypothesis cell carries the corrected reading.

## 5. The calibration check (task c) — producer, parameter-free, no fitting

`research/import-kw-01-calibrate.js`, embedded; guards: A113274 mod-6 +
monotone, and the TOS table's 75 record-starred rows must equal records 1..75
(VOID on one mismatch) — both passed; `Li₂` Simpson integrator anchored to
`π₂(10¹⁰)` and `π₂(10¹⁴)` at 0.1%. Findings, all in the embedded block:

- **[MEASURED] The trend is genuinely predictive raw**: `G/T̄ ∈ [0.78, 1.05]`
  from record 10 to record 82, thirteen decades, zero parameters. Records
  split 33 below `T_c` / 37 inside `[T_c, T̄]` / 12 above `T̄`; `G − T̄`
  changes sign 15 times along the ladder — both halves of eq (19) read as
  described at the resolution 82 correlated points allow.
- **[MEASURED] The ceiling creeps, never breaches**: 0 of 82 records reach
  eq (20)'s exact ceiling; ladder-third mean loads 0.544 → 0.685 → 0.759,
  worst 0.8463 (n = 75) — the Shanks shape (→ 1 from below).
- **[ADJUDICATED] 0.7574 vs 0.7504 is a RANGE ACCIDENT.** `0.7574 = 1/C_{2,H}`
  is the ceiling's COEFFICIENT (units ln³p); `0.7504` is the sweep's max
  LOAD (dimensionless fraction of ceiling used, `p_end ≤ 10¹¹` only). On the
  exact-constant ceiling the load reads 0.7530; six decades out the same
  statistic reads 0.8463; under their own eq (21) it tends to 1 while the
  coefficient stays put. Two unlike quantities passing each other. **No live
  sentence may cite the proximity as corroboration.**
- **[MEASURED] Bands**: `T̄(p²)/ln³p` at the sweep's band midpoints over-reads
  the embedded `c3` means by 6–13% (measured/trend 0.943, 0.904, 0.921,
  0.867), with `T_c` bracketing from below at the two upper bands —
  consistent with zonegap-01's "inside the ln³ family, between the trend
  forms, drifting constant". The height translation `s ~ p²` is ours (§2).
- **[MEASURED] Gumbel, qualitative only**: `h = (G − T_c)/a_c` over the 82
  records: mean 0.320, sd 1.156, skew **+0.588**, 59.8% positive — the
  right-skew sign matches Gumbel (reference +1.14); nothing stronger is
  claimed, because the ladder is one correlated running-max path, not their
  fixed-`x` many-residue-class ensemble, and their own p. 24 already grades
  the claim "open question".
- Record counts vs `N_c(x) < C log x`: 41/75/82 records at 10¹¹/10¹⁶/7·10¹⁶
  give `N/ln x` = 1.62 → 2.04 → 2.11, below any `C > 3`; consistent, no
  content either way.
- Advisory (disclosed, same floor as zonegap-01 §7): 4 READINGS figures in
  the producer are prose reformattings of block figures (1.05/1.0488,
  7e16/7.0479e16, 0.759/0.7588, 59.8/0.598), flagged by `embed.js --check`.

## 6. NOT REACHED

- The sibling model agent owns the staircase-correction FIT; nothing here
  fits anything, by design. Whether a KW-form prior beats an agnostic one on
  the correction field is that agent's question.
- Kourbatov 1309.4053 (record-gap tables) and the JIS 23 (2020)
  first-occurrence paper: still abstracts-only (recon NOT-REACHED stands);
  1309.4053 remains the candidate second witness for A113274's middle range.
- The Gumbel check on OUR per-zone spacings (as opposed to the record
  ladder's `h`-values) was not run — it needs the zone-level series with the
  staircase correlation modelled, which is the sibling's territory.
- MathSciNet/zbMATH sweeps for the row's field: not run this pass either;
  the row's absence claims rest on the recon's calibrated channels.
- The `q > 2` residue-class half of KW (their Tables 2–7, the `b(q,x)`
  empirical correction eqs (45)–(46)) was read but not calibrated against
  anything of ours: no zone object with residue structure exists yet.

---

*History layer: process record, staging. See `research/history/CHANGELOG.md`
for the corpus rule.*
